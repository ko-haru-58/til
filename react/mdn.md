# React：MDN学習記録
以下の章の、React に関するページを学習。
- [JavaScript フレームワークとライブラリー | MDN](https://developer.mozilla.org/ja/docs/Learn_web_development/Core/Frameworks_libraries)

（2026年2月からGihhubでの記録を開始しましたが、学習は以前からしていました。そのため記録は中途半端なところからスタートしています）

## 2026/02/27(金)
学習時間：30分  
  
usePrevios()がいつ呼び出されているのかという疑問について、const ⚪︎⚪︎ = usePrevious(●●); で宣言されているのは分かっていました。

Reactでは状態の更新があると全体がレンダリングされるので、変数の宣言が毎回呼び出されるとのことでした。やっと納得できました。

## 2026/02/25(水)
学習時間：30分  
  
コード全体をおおよそ把握しました。

その上でusePrevious() がいつ呼び出されているのか確認しましたが、何だか入れ子？無限ループ？になっているような気がしてこれで呼び出せるのかなぁと疑問です。

## 2026/02/24(火)
学習時間：30分  
  
usePrevious() がおおよそ分かったところで、どのタイミングで呼び出されているのかを調べています。

いつレンダリングされているのか全体像の把握をがんばっています。

## 2026/02/20(金)
学習時間：30分  
  
JavaScriptは関数などがDOMに紐づいていますが、Reactはそうではないっぽいので調べてみました。

Reactの内部の「Fiber」なるものに紐づくとのことです。まだFiberを理解するまでには至っていませんが、DOMじゃなくて別のところに紐づいている、と頭の片隅に入れておきます。

## 2026/02/19(木)
学習時間：30分  
  
useRef() について調べたついでに、以前でてきた、ref属性についても整理しました。

<div ref={myRef}>と書くと、このdivタグのDOMをmyRef.current にセットする、という指示になると改めて確認しました。

## 2026/02/18(水)
学習時間：30分  
  
usePrevious()の「const ref = useRef();」という記述で、呼び出されるたびにrefが初期化されると挙動がおかしくならないかなと疑問でした。
  
useRef() で宣言された変数は「レンダリングをまたいで保持される」という性質があり初期化されず既存値のままとなると知りました。

## 2026/02/17(火)
学習時間：30分  
  
usePrevious() のところに再び戻ってきました。ふわっとは分かるようになりました。色々と疑問も出てきたので明日以降調べます。

## 2026/02/16(月)
学習時間：30分  
  
先週に引き続き、各種フックについてドキュメントにまとめました。
  
## 2026/02/13(金)
学習時間：30分  

useEffect() の第2引数は「配列」と説明されているけれど、通常の文字列型や数値型のことも多いのになぜだろうと思っていました。  
  
宣言済みの配列を指定するイメージでしたが、[count, text]などのように、依存関係にあるものを[ ]で複数指定できる、ということを知りました。

## 2026/02/12(木)
学習時間：30分  

useRef() と useEffect() について、ドキュメントを作成してまとめました。

## 2026/02/10(火)
学習時間：30分  

引き続きusePrevious() について。

usePrevious() が難しいというよりも、該当の関数に含まれている useRef() と useEffect() の方でつまづいているような気がしてきました。

```
function usePrevious(value) {
  const ref = useRef();
  useEffect(() => {
    ref.current = value;
  });
  return ref.current;
}
```
https://developer.mozilla.org/ja/docs/Learn_web_development/Core/Frameworks_libraries/React_accessibility
<br>
より
<br><br>
次回は理解のためにこれまで出てきたフックをメモにまとめるところから始めるつもりです。

## 2026/02/05(木)
学習時間：30分  

usePrevious() の中にuseEffect() がある場合について確認中です。

usePrevious() はレンダリングの前の状態を保持するために使うものなのですが、usePrevious(){...}と書かれたときの{...}内の関数のことがよく分かっていないので、調べます。

## 2026/02/04(水)
学習時間：30分

useEffect() について「レンダリングのあとに実行したい関数を呼び出せる」と学習したのですが、その理解だけでは動きが分からず。どうやら思っていたよりももっと色々あって複雑なものみたいです。

## 2026/02/03(火)
学習時間：30分  

`<input ref={inputRef}>` で、`ref={inputRef}` は通常「refに`{inputRef}` を代入する」となりそうなのに、「`<input>` の DOM ノードを `inputRef.current` に入れる」となるのか疑問でした。

Reactではrefが「特別に解釈するキー」で、仕様で上記のような挙動になるとのことでした。

## 2026/02/02(月)
学習時間：30分  
  
以下のページについて
https://developer.mozilla.org/ja/docs/Learn_web_development/Core/Frameworks_libraries/React_accessibility

フォーカスの制御について、`<input ref={inputRef}>` のところが引っかかっているので調べました。

フォーカスを当てるのは1つなのに<input>の数だけinputRefが設定されてしまうのでは？重複してinputRefが設定されても問題ないのかな？と思いましたが、条件つきレンダリングで編集中の`<input>`は1つだけしか表示されないとのことで腑に落ちました。今回のケースは1つだけの設計なので問題ないようです。
